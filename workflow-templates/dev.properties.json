{
  "name": "WP Engine Dev Deploy",
  "description": "Deploys the theme to a WP Engine DEV install on every push to an open pull request. Add this only to a repo that has a dev install (typically an in-place rebuild). Requires the WPE_SSHG_KEY_PRIVATE secret and the WPE_DEV_ENV variable.",
  "iconName": "octicon-beaker",
  "categories": ["Deployment"]
}
